==================
Request header modification
==================

example.com reqHeaders://{headers.json}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
Response body replacement
==================

api.example.com resBody://{response.json}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
Method override
==================

example.com method://POST

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
User agent override
==================

example.com ua://Mozilla/5.0

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Status code modification
==================

example.com statusCode://404

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
HTML injection
==================

example.com htmlAppend://{script.html}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
CSS injection
==================

example.com cssPrepend://{custom.css}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
JavaScript injection
==================

example.com jsBody://{custom.js}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
Enable protocol
==================

example.com enable://capture

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Disable protocol
==================

example.com disable://cache

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Filter protocol
==================

example.com filter://keyword

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Ignore protocol
==================

example.com ignore://

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name))))

==================
Log protocol
==================

example.com log://{log.txt}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
Weinre debugging
==================

example.com weinre://debug

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Request delay
==================

example.com reqDelay://5000

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Response delay
==================

example.com resDelay://3000

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Request speed limit
==================

example.com reqSpeed://1024

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Response speed limit
==================

example.com resSpeed://512

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
CORS headers
==================

example.com reqCors://enable

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Redirect
==================

example.com redirect://https://new-site.com

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Proxy with authentication
==================

example.com proxy://user:pass@127.0.0.1:8080

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
SOCKS proxy
==================

example.com socks://127.0.0.1:1080

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
PAC proxy
==================

example.com pac://{proxy.pac}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))

==================
Host mapping
==================

example.com host://127.0.0.1

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Delete headers
==================

example.com delete://req.headers.cookie

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_text_start)))))

==================
Header replace
==================

example.com headerReplace://{replace.json}

---

(source_file
  (rule
    pattern: (domain_pattern)
    operator: (protocol_operator
      protocol: (protocol_name)
      value: (operator_value
        (value_reference_start
          (brace_open)
          name: (reference_name)
          (brace_close))))))
